---
description: Stage, commit, and push changes to origin/main automatically
---
// turbo-all
1. Stage all changes: `git add .`
2. Commit changes with a message: `git commit -m "update"`
3. Push to origin/main: `git push origin main`
